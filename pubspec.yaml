name: afrahna
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.33.1+34

environment:
  sdk: ^3.11.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  # Authentic social-media brand glyphs (TikTok / Instagram / WhatsApp / ...).
  font_awesome_flutter: ^11.0.0
  google_fonts: ^6.2.1
  intl: ^0.20.2
  dio: ^5.7.0
  shared_preferences: ^2.3.2
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.1
  provider: ^6.1.2
  url_launcher: ^6.3.0
  share_plus: ^10.1.2
  path_provider: ^2.1.4
  app_links: ^6.3.2
  speech_to_text: ^7.0.0
  # Held at 12.x on purpose. From 13.0.0 it pulls permission_handler_android
  # 14, whose build script requires AGP 9 / Gradle 9 / Kotlin 2.3 and
  # compileSdk 37 — this project is on AGP 8.11 / Gradle 8.14, so the plugin's
  # own Gradle script fails to compile before any app code is built. The only
  # call site is Permission.microphone.request(), which is the same in both.
  permission_handler: ^12.0.3
  video_player: ^2.9.2
  visibility_detector: ^0.4.0+2
  image_picker: ^1.1.2
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  geolocator: ^13.0.1
  firebase_core: ^4.10.0
  firebase_messaging: ^16.3.0
  flutter_local_notifications: ^22.0.1

# path_provider_foundation 2.6.0 switched to FFI via the `objective_c` package,
# which ships its dylib through Flutter's experimental native-assets mechanism.
# That dylib fails to load on the iOS simulator (DOBJC_initializeApi crash), so
# pin back to the last plugin-based release which has no objective_c dependency.
dependency_overrides:
  path_provider_foundation: 2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.1

# The launcher icon is built from icon/, not from assets/images/. These are
# build-time inputs only, and assets/images/ is bundled into the app — keeping
# them there shipped 1.4 MB of icon sources inside the download.
#
# The launcher icon is built from app_icon.png, not logo.png. logo.png carries
# a 15% transparent margin for use inside the app; as a launcher icon that
# margin turns white and the launcher then insets it again, which is why the
# icon sat small in a white square. app_icon.png is the same artwork bled to
# every edge.
#
# Android gets adaptive layers as well — a legacy PNG is shrunk onto a white
# plate by every launcher since Android 8, no matter how it is drawn. The
# artwork (background included) is the background layer, so the foreground is
# deliberately an empty image.
#
# Regenerate after changing the artwork:  dart run flutter_launcher_icons
flutter_launcher_icons:
  android: "ic_launcher"
  ios: true
  remove_alpha_ios: true
  image_path: "icon/app_icon.png"
  # Pre-inset: Android draws this layer at 108dp and guarantees only the
  # middle 66dp, so the full-bleed art would have its wordmark cropped.
  adaptive_icon_background: "icon/app_icon_adaptive_bg.png"
  adaptive_icon_foreground: "icon/app_icon_fg.png"
  min_sdk_android: 21
  web:
    generate: true
    image_path: "icon/app_icon.png"
  windows:
    generate: true
    image_path: "icon/app_icon.png"
    icon_size: 256

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/video/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
